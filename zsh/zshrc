ZSH=$HOME/.oh-my-zsh

ZSH_THEME="robbyrussell"

# Aliases
alias be="bundle exec"
alias dspace="df -h -T"
alias v="vim"

# Settings
DISABLE_AUTO_UPDATE="true"

# Plugins
plugins=(git)

source $ZSH/oh-my-zsh.sh
unsetopt correct_all

[[ -s "$HOME/.rvm/scripts/rvm" ]] && . "$HOME/.rvm/scripts/rvm" # Load RVM function

# Customize to your needs...
export PATH=/usr/lib/lightdm/lightdm:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/games:/home/madundead/.rvm/bin:/home/madundead/.rvm/bin
